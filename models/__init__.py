'''
membrane_potential: represents how much electrical charge is currently inside the neuron
resting_potential: the baseline value that the neuron naturally wants to return to when no inputs are happening
threshold: the strict limit the voltage must cross to trigger a spike
leak_factor: a parameter that defines how fast the neuron loses its charge
'''
threshold = 1.0             # variable factor
leak_factor = 0.1           # variable factor